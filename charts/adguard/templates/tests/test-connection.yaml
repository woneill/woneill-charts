apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "adguard.fullname" . }}-test-connection"
  labels:
    {{- include "adguard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "adguard.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
